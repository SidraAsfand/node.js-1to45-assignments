"use strict";
//12.Greetings: Start with the array you used in Exercise 11, but instead of just printing each person’s name, print a message to them. The text of each message should be the same, but each message should be personalized with the person’s name.
let array_names = ['sidra', 'sana', 'nimra', 'shafaq', 'sabeen', 'areeba'];
let message = "Hello, how are you?";
console.log(`${message} ${array_names[0]}`);
console.log(`${message} ${array_names[1]}`);
console.log(`${message} ${array_names[2]}`);
console.log(`${message} ${array_names[3]}`);
console.log(`${message} ${array_names[4]}`);
console.log(`${message} ${array_names[5]}`);
